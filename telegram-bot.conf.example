# Store the real file at /switch/nxgallery/telegram-bot.conf on the SD card.
# Never package or commit the real bot token.
bot_token=123456789:replace_me
discover_chats=true

# Optional configured entries appear immediately, before the bot observes updates.
# Repeat this shape for every known destination: chat=CHAT_ID|Chat title
